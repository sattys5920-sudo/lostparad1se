// 겨울 저녁 학교의 색. **이 파일 하나가 정본이다.**
//
// 맵도 UI도 스프라이트도 여기를 본다. 전에는 맵 팔레트(sprites.ts)와
// UI 색(controls.css)이 따로 살았고, 그래서 바닥이 거의 흰색인데
// 조작부는 짙은 남색이라 **화면이 위아래로 갈라져 보였다.** 같은 게임의
// 두 부분이 아니라 서로 다른 두 그림 같았다.
//
// 지금은 맵도 차가운 회청색이다. UI 는 그보다 어둡지만 **같은 색상
// 계열**이라, 아래로 갈수록 저녁이 깊어지는 한 장면으로 읽힌다.
//
// CSS 는 이 파일을 못 읽으므로 같은 값을 skin.css 에도 적어 둔다.
// 둘이 어긋나면 시험이 잡는다(skin.test.ts).

/** 맵 — 면과 선. */
export const MAP = {
  /** 방 바닥. 흰색이 아니라 차가운 회청색이다 */
  floor: '#c8cdd8',
  /** 바닥 타일 무늬. 아주 옅게만 — 무늬가 보이면 사람이 안 보인다 */
  floorTile: '#bec3ce',
  /** 실외(옥상·운동장·정원). 눈이 덮여 한 단계 밝다 */
  floorOut: '#d4d8e0',
  /** 복도. 방보다 어두워서 안과 밖이 갈린다 */
  floorHall: '#8a90a4',
  /** 벽 몸통. 순수 검정이 아니다 */
  wall: '#2a2f42',
  /** 벽 윗면 1px. 두께가 생긴다 */
  wallLit: '#3a4058',
  /** 모든 외곽선. 검정 대신 이것 — 배경과 톤이 맞는다 */
  outline: '#1a1d2e',
  /** 스프라이트 중간 톤과 밝은 면 */
  mid: '#5c646b',
  light: '#c9ced2',
} as const

/** UI — 맵보다 어둡되 같은 계열. */
export const UI = {
  bg: '#1c1f33',
  face: '#353a57',
  line: '#4a5070',
  /** 베벨 — 위·왼쪽은 밝고 아래·오른쪽은 어둡다 */
  bevelLit: '#4a5070',
  bevelDim: '#0e1120',
  text: '#e8e6f5',
  gold: '#f0d68a',
  ice: '#9fd4e8',
  down: '#e07a72',
  /** 로그 본문. 이름은 팀 완장 색이 맡는다 */
  logText: '#d8dce8',
} as const

/** 종이 — 투표용지·쪽지·이벤트 화면이 같은 종이를 쓴다. */
export const PAPER = {
  sheet: '#f4f1e8',
  ink: '#1a1d2e',
  rule: '#c2564e',
} as const

/**
 * 글자 크기는 **세 단계뿐이다.**
 *
 * 픽셀 글꼴은 정수 배율에서만 또렷하다. 여기 없는 크기를 쓰면 반드시
 * 어딘가에서 뭉갠다 — 8px 을 1.5배 해서 12를 만드는 식이 제일 나쁘다.
 */
export const TYPE = {
  /** 타이머·방 이름 */
  big: 16,
  /** 본문 */
  body: 12,
  /** 라벨·캡션·이름표 */
  small: 9,
} as const
